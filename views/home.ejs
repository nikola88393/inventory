<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <h1>Home</h1>
    <a href="/categories/new">Dobavi kategoriq</a>
    <ul>
      <% if (data) { %> <% data.forEach((product) => { %>
      <li><%= product.name %> = <%= product.price %></li>
      <% }) %> <% } %>
    </ul>
  </body>
</html>
